# Offline conversion only — never a product runtime dependency.
huggingface_hub>=0.26.0
gguf>=0.10.0
torch>=2.2.0
safetensors>=0.4.0
numpy>=1.26.0
tqdm>=4.66.0
# macOS optional CoreML export (not required for GGUF):
# coremltools>=7.0
